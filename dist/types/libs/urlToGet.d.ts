/**
  * 为url链接拼接参数
  * @param url - url地址
  * @param searchString - 查询参数
  * @returns 拼接后的url地址
  */
export default function urlToGet(url: string, searchString: string | Record<string, any>): string;
