/**
   * 向后填充字符串
   * @param data - 原始目标
   * @param len - 需要填充的总长度
   * @param target - 填充的字符串
   * @returns 结果
   */
export default function padEnd(data: string | number, len: number, target: string): string;
