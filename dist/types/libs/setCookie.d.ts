/**
 * 设置cookie。默认path="/"
 * @param key - 设置的键名
 * @param value - 设置的值
 * @param expiredTime - 单位（毫秒）
 */
export default function setCookie(key: string, value: string, expiredTime?: number): void;
